{
	"name": "binlist-data",
	"repository": "binlist/data",
	"scripts": {
		"check-ranges": "node ./tools/check-ranges < ./ranges.csv",
		"test": "npm run -s check-ranges"
	},
	"dependencies": {
		"csv-parser": "^1.11.0",
		"iso-3166-1-codes": "^1.0.0"
	}
}
